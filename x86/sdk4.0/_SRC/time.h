#ifndef TIME_H

#define TIME_H

//#include "timer.h"

extern WORD GetTime(DPTR);	// Получить время
extern WORD SetTime(DWORD);	// Установить время
extern WORD GetSystemRawTime(void);	// Функция для определения  
					// точных временных интервалов.
        

#endif


